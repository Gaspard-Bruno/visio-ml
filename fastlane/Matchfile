git_url('git@github.com:Gaspard-Bruno/apple-dev-credentials.git')

storage_mode('git')

type('development') # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(ENV['APP_BUNDLE_ID'])
